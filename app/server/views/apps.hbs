<hr>

<table>
    {{#each files.apk}}
        {{#each this}}
            <tr>
                <td>{{this.label}} {{this.version}}</td>
                <td class="file">{{this.file}}</td>
                <td>
                    {{#if this.orig}}
                        <a href="{{this.orig}}">скачать</a>
                    {{/if}}
                </td>
                <td>
                    {{#if this.relativePath}}
                        <a href="{{this.relativePath}}">зеркало</a>
                    {{/if}}
                </td>
            </tr>
        {{/each}}
        <tr>
            <td>&nbsp;</td>
        </tr>
    {{/each}}
</table>

<hr>

<div class="info">
    <p>
        Обновление приложений:
        <span class="timestamp">{{data.texts.timestamp}} :: {{files.count}}/{{data.texts.providers.count}}</span>
        <br>
        Визиты:
    </p>
    <p>
        {{#each data.visitors.useragents}}
            > {{this}}
            <br>
        {{/each}}
    </p>
</div>
