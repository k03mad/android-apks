<h1>{{pageData.texts.header}}</h1>

<table>
    {{#each apkFiles}}
        <tr>
            <td>{{this.label}} {{this.version}}</td>
            <td>
                <a href="{{this.relativePath}}">{{this.file}}</a>
            </td>
        </tr>
    {{else}}
        <tr>
            <td class="error">Приложения ещё не скачивались</td>
        </tr>
    {{/each}}
</table>

<div class="info">
    <p>
        Обновление приложений:
        <span class="timestamp">{{pageData.texts.timestamp}}</span>
    </p>
    <p>Визиты:</p>
    {{#each pageData.visitors.useragents}}
        > {{this}}</br>
    {{/each}}
</div>
